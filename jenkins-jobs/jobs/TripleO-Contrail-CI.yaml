- job:
    name: TripleO-Contrail-CI
    description: 'TripleO Contrail CI'
    defaults: global
    node: slave1
    concurrent: true
    parameters:
      - choice:
          name: NUM
          description: "Enironment Nubmer"
          choices:
            - '3'
            - '4'
            - '5'
            - '6'
      - choice:
          name: ENVIRONMENT_OS
          description: "CentOS or RHEL based installation"
          choices:
            - 'centos'
            - 'rhel'
      - choice:
          name: OPENSTACK_VERSION
          description: "Version of the OpenStack"
          choices:
            - 'ocata'
            - 'newton'
            - 'mitaka'
      - choice:
          name: CONTROLLER_COUNT
          description: "Count of OS controllers to deploy"
          choices:
            - '1'
            - '3'
            - '5'
      - choice:
          name: CONTRAIL_CONTROLLER_COUNT
          description: "Count of contrail controllers to deploy (controller - deploy to OS controller)"
          choices:
            - '1'
            - '3'
            - 'controller'
      - choice:
          name: CONTRAIL_ANALYTICS_COUNT
          description: "Count of contrail analytics to deploy (controller - deploy to OS controller)"
          choices:
            - '1'
            - '3'
            - 'controller'
      - choice:
          name: CONTRAIL_ANALYTICSDB_COUNT
          description: "Count of contrail analytics DB to deploy (controller - deploy to OS controller)"
          choices:
            - '1'
            - '3'
            - 'controller'
      - choice:
          name: CLEAN_ENV
          choices:
            - 'auto'
            - 'always'
            - 'never'
      - choice:
          name: RHEL_CERT_TEST
          choices:
            - 'no'
            - 'yes'

    scm:
      - juniper-ci
    builders:
      - shell: |
          #!/bin/bash -e
          desc="$OPENSTACK_VERSION $ENVIRONMENT_OS"
          if [[ "$RHEL_CERT_TEST" == 'yes' ]] ; then
            if [[ "$ENVIRONMENT_OS" != 'rhel' ]] ; then
              echo "RedHat Certification is available only for RHEL environment"
              exit -1
            fi
            desc+=" RedHat Certification"
          fi
          desc+="\n(num=$NUM osc=$CONTROLLER_COUNT cc=$CONTRAIL_CONTROLLER_COUNT ca=$CONTRAIL_ANALYTICS_COUNT cadb=$CONTRAIL_ANALYTICSDB_COUNT)"
          echo "DESCRIPTION $desc"
      - description-setter:
          regexp: "DESCRIPTION (.*)"
      - shell: |
          #!/bin/bash -e
          ./juniper-ci/tripleo/run-contrail.sh
    publishers:
      - archive:
          artifacts: 'logs/**'
          allow-empty: 'true'
