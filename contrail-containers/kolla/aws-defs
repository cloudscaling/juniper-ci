export NET_COUNT=1
if [[ "$HA" != 'aio' ]] ; then
  echo "ERROR: Only AIO deployment is supported"
  exit 1
fi

export BUILD_NODE_TYPE=m4.large
export CONT_NODES=1
export CONT_NODE_TYPE=c4.4xlarge
export COMP_NODES=0
export COMP_NODE_TYPE=''
