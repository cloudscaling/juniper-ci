export NET_COUNT=1
export NET_BASE_PREFIX=45

# we always need to create machine for containers build if build is required
export BUILD_NODE_MEM=2048

if [[ "$HA" == 'nonha' ]]; then
  export CONT_NODES=1
else
  echo "ERROR: HA and AIO deployments are not supported"
  exit 1
fi
export CONT_NODE_MEM=32768
export COMP_NODES=0
export COMP_NODE_MEM=6144
